diff -up gst-plugins-good-1.14.1/ext/meson.build.2~ gst-plugins-good-1.14.1/ext/meson.build
--- gst-plugins-good-1.14.1/ext/meson.build.2~	2018-03-23 21:44:48.000000000 +0100
+++ gst-plugins-good-1.14.1/ext/meson.build	2018-06-10 13:43:35.595727110 +0200
@@ -14,7 +14,7 @@ endif
 subdir('libpng')
 subdir('mpg123')
 subdir('raw1394')
-# FIXME: subdir('qt')
+subdir('qt')
 subdir('pulse')
 subdir('shout2')
 subdir('soup')
diff -up gst-plugins-good-1.14.1/ext/qt/meson.build.2~ gst-plugins-good-1.14.1/ext/qt/meson.build
--- gst-plugins-good-1.14.1/ext/qt/meson.build.2~	2018-06-10 13:43:35.595727110 +0200
+++ gst-plugins-good-1.14.1/ext/qt/meson.build	2018-06-10 13:54:53.958764268 +0200
@@ -0,0 +1,36 @@
+qt5 = import('qt5')
+qt5_dep = dependency('qt5', modules: ['Core', 'Gui', 'Quick'])
+
+moc_files = qt5.preprocess(moc_headers: ['gstqsgtexture.h', 'qtitem.h', 'qtwindow.h'])
+
+qmlgl_sources = [
+  'gstplugin.cc',
+  'gstqsgtexture.cc',
+  'gstqtglutility.cc',
+  'gstqtsink.cc',
+  'gstqtsrc.cc',
+  'qtitem.cc',
+  'qtwindow.cc',
+  moc_files
+]
+
+if qt5_dep.found() and add_languages('cpp', required : false)
+  extra_args = []
+  cxx = meson.get_compiler('cpp')
+  #if cxx.has_argument('-fvisibility=hidden') MESON IS BROKEN
+  #  extra_args += ['-fvisibility=hidden']
+  #endif
+  #if cxx.has_argument('-fno-strict-aliasing')
+  #  extra_args += ['-fno-strict-aliasing']
+  #endif
+
+  gstqmlgl = library('gstqmlgl',
+    qmlgl_sources,
+    c_args : gst_plugins_good_args + ['-DGST_USE_UNSTABLE_API'],
+    cpp_args : gst_plugins_good_args + ['-DGST_USE_UNSTABLE_API'] + extra_args,
+    include_directories : [configinc, libsinc, include_directories('/usr/include/glib-2.0'), include_directories('/usr/lib64/glib-2.0/include'), include_directories('/usr/include/gstreamer-1.0'), include_directories('/usr/lib64/gstreamer-1.0/include')],
+    dependencies : [gstbase_dep, gstgl_dep, qt5_dep],
+    install : true,
+    install_dir : plugins_install_dir,
+  )
+endif
